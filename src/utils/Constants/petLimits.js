/** Maximum pets a single logged-in user may have in Milo Web. */
export const MAX_PETS_PER_USER = 10;

/** Friendly notice when the limit is reached (shown in UI + toast). */
export const PET_LIMIT_MESSAGE =
  "Thanks for loving pets so much! You are not allowed to add more than 10 pets under one username.";
